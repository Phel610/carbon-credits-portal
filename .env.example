# Development configuration
VITE_QA_MODE=true

# Parity path configuration (optional - defaults to current working directory)
PARITY_ROOT_DIR=.
PARITY_DIR=./parity
PARITY_FIXTURES_DIR=./parity/fixtures

# Server configuration
PORT=4000
NODE_ENV=development